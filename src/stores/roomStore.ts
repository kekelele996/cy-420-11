import { defineStore } from 'pinia'; import type { Room } from '../models/room'; import { websocketService } from '../services/websocketService'; import { WsMessageType } from '../constants/websocket'; import { PlayerStatus, PLAYER_COLORS } from '../constants/player'; import { logGame } from '../utils/gameLogger'; export const useRoomStore=defineStore('room',{state:()=>({rooms:websocketService.rooms as Room[],currentRoom:undefined as Room|undefined}),actions:{refresh(){logGame('ROOM_LIST'); websocketService.send(WsMessageType.ROOM_LIST,{}); this.rooms=websocketService.rooms;},create(name='我的房间'){this.currentRoom=websocketService.createRoom(name);this.currentRoom.players.push({id:'p-local',nickname:'你',color:PLAYER_COLORS[0],score:0,status:PlayerStatus.ALIVE,position:{x:5,y:5},trail:[],territory:[]});this.rooms=websocketService.rooms;return this.currentRoom.id;},join(id:string){this.currentRoom=this.rooms.find(r=>r.id===id);if(this.currentRoom&&!this.currentRoom.players.find(p=>p.id==='p-local')){this.currentRoom.players.push({id:'p-local',nickname:'你',color:PLAYER_COLORS[0],score:0,status:PlayerStatus.ALIVE,position:{x:5,y:5},trail:[],territory:[]});}logGame('ROOM_JOIN',{id});}}});
